# Signals that this entity represents a callable. 
# a Global component on the same entity indicates that call results could have global effect, and the node may want to prioritize these methods on a UI

seq RPCEndpointComponent
- name: RPCName
- arguments: RPCArgumentHintList

dyn_array RPCName : u8 * u8

dyn_array RPCArgumentHintList : u8 * RPCArgumentHint

seq RPCArgumentHint
- name: RPCName
- type: AnyTypeID